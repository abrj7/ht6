# --- Person C fills this in ---
# FreeSolo OpenAI-compatible inference. Leave any blank to run in stub mode
# (number-grounded coaching templates + Suite Hearts persona stubs).
# Base URL and model are the real deployed yonder-coach LoRA adapter - not
# secret, safe to commit. FREESOLO_API_KEY is the secret - get it from
# whoever has yonder-ht6 org access and put it in your own untracked .env.
FREESOLO_API_KEY=
FREESOLO_BASE_URL=https://clado-ai--freesolo-lora-serving.modal.run/v1
FREESOLO_MODEL=flash-1784433096-6a060015

# Legacy alias used in older docs — prefer FREESOLO_BASE_URL above.
FREESOLO_API_URL=

PORT=5001
